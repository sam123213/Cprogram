// check which no. is big

#include<stdio.h>
#include<conio.h>
void main()
{
    int a,b;
    printf("Enter two No.:");
    scanf("%d %d",&a,&b);
    if (a>=b)
    {
       printf("A no. is big");
    }
    else
    {
        printf("B no. is big");
    }
}